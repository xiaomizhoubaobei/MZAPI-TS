interface:
  display_name: "MZAPI TS"
  short_description: "Repo-specific patterns and workflows for MZAPI-TS"
  default_prompt: "Use the MZAPI-TS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true